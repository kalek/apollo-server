extend type Query {
  product(id: ID!): Product
  products: [Product]
}

type Product {
  id: ID!
  name: String!
  price: Float!
  category: String!
  coverImage: Image
}
type Image {
  src: String!
  alt: String!
}
